git 


git diff --cached
git reset --hard file_chaged
git restore file -->to unstage

#to add in stage

git add

#for commit

git commit --amend
git commit -m "message"


#to see add commit logs
git log
 

#branch

=>to see 
git branch

=>to create and switch
git checkout -b new-branch-name

//or

git branch new-branch
git checkout new-branch

after staging
 git push -u origin new-branch

git push --forse-with-lease
 
 git push


git remote -v

to resolve
git checkout master
git pull
git checkout to existing branch

to resolve changing to main branch

git rebase master
git rebase --continue

